---
sidebar_label: Exercices simples
sidebar_position: 98
description: Exercices simples sur les fonctions
---

# Exercices simples sur les fonctions

<ExerciseList>
<Exercise>
  <ExerciseHeader>
  ### Multiple de trois ou cinq
  </ExerciseHeader>
  <ExerciseBody>
  Écrire une fonction qui accepte un nombre entier et qui retourne `True` si le nombre
    est un multiple de trois ou de cinq, et `False` sinon.
```python
def multiple_de_trois_ou_cinq(n):
    # Écrire votre code ici
```
   </ExerciseBody>
  <ExerciseFooter>
    Par exemple, `multiple_de_trois_ou_cinq(15)` devrait retourner `True`,
    tandis que `multiple_de_trois_ou_cinq(7)` devrait retourner `False`.
  </ExerciseFooter>
</Exercise>

<Exercise> 
  <ExerciseHeader>
  ### Vérifier un palindrome
  </ExerciseHeader>
  <ExerciseBody>
  Écrire une fonction qui vérifie (retourne `True`) si une chaîne de caractères est un palindrome,
  c'est-à-dire qu'elle se lit de la même manière dans les deux sens. 

  ```python
  def est_palindrome(chaine): 
      # Écrire votre code ici
  ```
  </ExerciseBody>
  <ExerciseFooter>
    Par exemple, `est_palindrome("radar")` devrait retourner `True`,
    tandis que `est_palindrome("python")` devrait retourner `False`.
  </ExerciseFooter>
</Exercise> 

<Exercise> 
  <ExerciseHeader>
  ### Générer une liste de carrés
  </ExerciseHeader>
  <ExerciseBody> 
  Écrire une fonction qui prend en paramètre une liste d'entiers et retourne
  une nouvelle liste contenant le carré de chaque nombre.

```python
def liste_des_carres(liste):
    # Écrire votre code ici
```
  </ExerciseBody>
  <ExerciseFooter>
    Par exemple, `liste_des_carres([1, 2, 3])` devrait retourner `[1, 4, 9]`.
  </ExerciseFooter>
</Exercise> 

<Exercise> 
  <ExerciseHeader>
  ### Somme des chiffres
  </ExerciseHeader>
  <ExerciseBody> 
  Écrire une fonction qui prend un entier et retourne la somme de ses chiffres.
```python
def somme_des_chiffres(n: int):
    # Écrire votre code ici
```
</ExerciseBody>
  <ExerciseFooter>
    Par exemple, `somme_des_chiffres(123)` devrait retourner `6`.
  </ExerciseFooter>
 </Exercise> 

 <Exercise> 
 <ExerciseHeader>
 ### Trouver le troisième plus grand nombre
 </ExerciseHeader>
 <ExerciseBody> 
 Écrire une fonction qui prend une liste d'entiers et retourne le troisième plus grand nombre de la liste.
 La fonction retournera `None` si la liste contient moins de trois éléments.
 ```python
def troisieme_plus_grand(liste):
    # Écrire votre code ici 
``` 
</ExerciseBody> 
  <ExerciseFooter>
    Par exemple, `troisieme_plus_grand([1, 2, 3, 4, 5])` devrait retourner `3`,
    tandis que `troisieme_plus_grand([5, 4])` devrait retourner `None`.
  </ExerciseFooter>
</Exercise>
</ExerciseList>